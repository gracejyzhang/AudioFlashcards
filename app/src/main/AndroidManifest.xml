<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.audioflashcards">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.FlashcardActivity"></activity>
        <activity android:name=".activities.SelectRecyclerActivity"></activity>
        <activity android:name=".activities.AddFlashcardActivity"></activity>
        <activity android:name=".activities.AddDeckActivity"></activity>
        <activity android:name=".activities.SingleSelectActivity"></activity>
        <activity android:name=".activities.EditFlashcardActivity"></activity>
        <activity android:name=".activities.BaseActivity"></activity>
    </application>

</manifest>